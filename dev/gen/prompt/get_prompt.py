def rag_verify_feedback_prompt(des: str, code: str, feedback: str, context: str):
    return """
    ### Role:
    You are an expert in the Lustre language and proficient in using the Kind2 formal verification tool.。
    ### Mission:
    Please strictly refer to the following knwoledge context to generate Lustre code: """+f"{context}" + """
    Generate Lustre code based on the following information:
    1. Your answer should only include the Lustre code generated by the requirement; 
    2. Markdown code blocks, such as ```, are not required. 生成的内容不需要包含markdown代码块格式；
    3. Analyze the requirement carefully; do not omit or over-include any information.
    """ + f"\nThe requirement for your mission is: {des} \n And the following code failed verification: {code}\n The verification feedback information is: {feedback}\n" + """ 
    
    ### Mission Example：
    #### Here is the requirement:
    Define the top node:
    - Input: Accepts a boolean input "reset"
    - Output: Returns a boolean output "OK"
    - Internal variables:
      - i: an integer internal variable used to record the time count value
    - Functional logic: At the initial moment, the value of i is 1; at subsequent moments, the value of i is equal to the value of i at the previous moment plus 1
    - Verification rules:
      - OK: Whether the value of variable i is greater than or equal to 0. Use Kind2 verification syntax to check if this holds consistently
    
    #### The generated Lustre model is：
    node top (reset: bool) returns (OK: bool);
    var i : int;
    let
        i = 1 -> pre i + 1;
        OK = i >= 0;
        --%PROPERTY OK;
    tel"""


def rag_syntax_feedback_prompt(des: str, code: str, feedback: str, context: str):
    return """
    ### Role:
    You are an expert in the Lustre language and proficient in using the Kind2 formal verification tool.。
    ### Mission:
    Please strictly refer to the following knwoledge context to generate Lustre code: """ + f"{context}" + """
    Generate Lustre code based on the following information:
    1. Your answer should only include the Lustre code generated by the requirement; Markdown code blocks are not required.
    2. Analyze the requirement carefully; do not omit or over-include any information.
    """ + f"\nThe requirement for your mission is: {des} \n And the following code failed syntax checking: {code}\n The syntax checking feedback information is: {feedback}\n" + """ 

    ### Mission Example：
    #### Here is the requirement:
    Define the top node:
    - Input: Accepts a boolean input "reset"
    - Output: Returns a boolean output "OK"
    - Internal variables:
      - i: an integer internal variable used to record the time count value
    - Functional logic: At the initial moment, the value of i is 1; at subsequent moments, the value of i is equal to the value of i at the previous moment plus 1
    - Verification rules:
      - OK: Whether the value of variable i is greater than or equal to 0. Use Kind2 verification syntax to check if this holds consistently

    #### The generated Lustre model is：
    node top (reset: bool) returns (OK: bool);
    var i : int;
    let
        i = 1 -> pre i + 1;
        OK = i >= 0;
        --%PROPERTY OK;
    tel"""


def rag_prompt(des: str, context: str):
    return """
    ### Role:
    You are an expert in the Lustre language and proficient in using the Kind2 formal verification tool.。
    ### Mission:
    Please strictly refer to the following knwoledge context to generate Lustre code: """ + f"{context}" + """
    Generate Lustre code based on the following information:
    1. Your answer should only include the Lustre code generated by the requirement; 
    2. Markdown code blocks are not required. 生成的内容不需要包含markdown代码块格式；
    3. Analyze the requirement carefully; do not omit or over-include any information.
    """ + f"\nThe requirement for your mission is: {des}\n" + """ 

    ### Mission Example：
    #### Here is the requirement:
    Define the top node:
    - Input: Accepts a boolean input "reset"
    - Output: Returns a boolean output "OK"
    - Internal variables:
      - i: an integer internal variable used to record the time count value
    - Functional logic: At the initial moment, the value of i is 1; at subsequent moments, the value of i is equal to the value of i at the previous moment plus 1
    - Verification rules:
      - OK: Whether the value of variable i is greater than or equal to 0. Use Kind2 verification syntax to check if this holds consistently

    #### The generated Lustre model is：
    node top (reset: bool) returns (OK: bool);
    var i : int;
    let
        i = 1 -> pre i + 1;
        OK = i >= 0;
        --%PROPERTY OK;
    tel"""


def verify_feedback_prompt(des: str, code: str, feedback: str):
    return """
    ### Role:
    You are an expert in the Lustre language and proficient in using the Kind2 formal verification tool.。
    ### Mission:
    Generate Lustre code based on the following information:
    1. Your answer should only include the Lustre code generated by the requirement; Markdown code blocks are not required.
    2. Analyze the requirement carefully; do not omit or over-include any information.
    """ + f"\nThe requirement for your mission is: {des} \n And the following code failed verification: {code}\n The verification feedback information is: {feedback}\n" + """ 

    ### Mission Example：
    #### Here is the requirement:
    Define the top node:
    - Input: Accepts a boolean input "reset"
    - Output: Returns a boolean output "OK"
    - Internal variables:
      - i: an integer internal variable used to record the time count value
    - Functional logic: At the initial moment, the value of i is 1; at subsequent moments, the value of i is equal to the value of i at the previous moment plus 1
    - Verification rules:
      - OK: Whether the value of variable i is greater than or equal to 0. Use Kind2 verification syntax to check if this holds consistently

    #### The generated Lustre model is：
    node top (reset: bool) returns (OK: bool);
    var i : int;
    let
        i = 1 -> pre i + 1;
        OK = i >= 0;
        --%PROPERTY OK;
    tel"""


def syntax_feedback_prompt(des: str, code: str, feedback: str):
    return """
    ### Role:
    You are an expert in the Lustre language and proficient in using the Kind2 formal verification tool.。
    ### Mission:
    Generate Lustre code based on the following information:
    1. Your answer should only include the Lustre code generated by the requirement; Markdown code blocks are not required.
    2. Analyze the requirement carefully; do not omit or over-include any information.
    """ + f"\nThe requirement for your mission is: {des} \n And the following code failed syntax checking: {code}\n The syntax checking feedback information is: {feedback}\n" + """ 

    ### Mission Example：
    #### Here is the requirement:
    Define the top node:
    - Input: Accepts a boolean input "reset"
    - Output: Returns a boolean output "OK"
    - Internal variables:
      - i: an integer internal variable used to record the time count value
    - Functional logic: At the initial moment, the value of i is 1; at subsequent moments, the value of i is equal to the value of i at the previous moment plus 1
    - Verification rules:
      - OK: Whether the value of variable i is greater than or equal to 0. Use Kind2 verification syntax to check if this holds consistently

    #### The generated Lustre model is：
    node top (reset: bool) returns (OK: bool);
    var i : int;
    let
        i = 1 -> pre i + 1;
        OK = i >= 0;
        --%PROPERTY OK;
    tel"""


def init_prompt(des: str):
    return """
    ### Role:
    You are an expert in the Lustre language and proficient in using the Kind2 formal verification tool.。
    ### Mission:
    Generate Lustre code based on the following information:
    1. Your answer should only include the Lustre code generated by the requirement; Markdown code blocks are not required.
    2. Analyze the requirement carefully; do not omit or over-include any information.
    """ + f"\nThe requirement for your mission is: {des}\n" + """ 

    ### Mission Example：
    #### Here is the requirement:
    Define the top node:
    - Input: Accepts a boolean input "reset"
    - Output: Returns a boolean output "OK"
    - Internal variables:
      - i: an integer internal variable used to record the time count value
    - Functional logic: At the initial moment, the value of i is 1; at subsequent moments, the value of i is equal to the value of i at the previous moment plus 1
    - Verification rules:
      - OK: Whether the value of variable i is greater than or equal to 0. Use Kind2 verification syntax to check if this holds consistently

    #### The generated Lustre model is：
    node top (reset: bool) returns (OK: bool);
    var i : int;
    let
        i = 1 -> pre i + 1;
        OK = i >= 0;
        --%PROPERTY OK;
    tel"""



