定义test节点：  
- R1: 输入：接收一个整数类型的输入x
- 输出：
  - R2: 返回一个包含两个整数元素的数组类型的输出y，类型为int^2
  - R3: 返回一个包含两个整数元素的数组类型的输出z，类型为int^2
  - R4: 返回一个布尔类型的输出P
- 内部变量：  
  - R5: OK：一个布尔类型的内部变量，用于记录验证规则的状态
- 功能逻辑：  
  - R6: y被初始化为一个包含两个元素且每个元素值为0的整数数组，即[0, 0]
  - R7: z被初始化为一个包含两个元素且每个元素值为0的整数数组，即[0, 0]
  - R8: P的值被计算为布尔表达式(y = z)的结果，即判断y和z是否相等
- 验证规则：  
  - R9: OK：P始终为真，使用形式化验证工具检测该规则是否恒成立