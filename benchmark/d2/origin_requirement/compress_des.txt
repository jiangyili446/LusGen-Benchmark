Define two nodes, node X and node Y. Node X receives a Boolean input i and returns an integer x. Internally, node X defines a variable OK to indicate the validity of the state. The calculation logic for x is as follows: initially, at time 0, if the previous time's x is less than 2, then the value of x is 1 minus the previous time's x; if the previous condition is not met and input i is true, then the value of x is 3; in other cases, the value of x is set to 2. Additionally, the variable OK is used to check whether the value of x is less than 3, and a kind2 verification entry is declared to ensure that this condition is always met.

Node Y receives two Boolean inputs i and clk and returns an integer x. Node Y defines a variable OK. The calculation logic for x is: when clk is true, Node Y calls Node X and provides i as the input; when clk is false, the value of x is 0. The OK variable in Node Y is also used to check whether x is less than 3, and a kind2 verification entry is declared to ensure that this condition should always hold in all cases.