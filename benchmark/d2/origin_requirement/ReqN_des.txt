Define a node N that receives an integer input I and an integer input X, and returns an integer result Z. The computation logic for Z inside node N is: if input I is greater than 0, then Z is equal to I plus 2; if input I is not greater than 0, Z is equal to I plus X.

Next, define a node ReqN, which receives three integer inputs I, X1, and X2, and returns a boolean value P. The logic of node ReqN requires I to always be positive and allows X1 and X2 to be any values (and they can be different). P is true if and only if, for any different X1 and X2, node N returns the same output when input I is positive, i.e., N(I, X1) equals N(I, X2). A main property is declared here to ensure that P's condition holds in all cases.