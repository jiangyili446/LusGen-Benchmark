节点top接收一个布尔输入reset，输出一个整型i和一个布尔量OK。

i的计算逻辑为：在初始时刻i取值为1；从下一时刻开始，i取上一时刻的值并乘以1024，即i按周期以1024倍递增（1，1024，1048576，…）。该节点中reset输入未参与任何计算，对i和OK不产生影响。

OK的计算逻辑为：当且仅当i大于等于1时，OK为真。

该节点声明了一个性质要求：在所有时刻OK必须成立，也即i在所有时刻都必须满足i ≥ 1。