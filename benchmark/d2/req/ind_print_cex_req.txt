节点bla接收一个实数输入in，输出一个实数out，并在内部定义了一个临时变量tmp（实数类型）用于保存历史输入值。

其行为要求如下：
1) tmp用于保存输入in的上一拍值：在初始时刻tmp取in的初始值；从第二拍开始，tmp等于上一拍的in。
2) out用于输出延迟两拍的in：在初始时刻out取in的初始值；从第二拍开始，out等于上一拍的tmp，也就是等价于两拍之前的in。

此外，该节点声明了一个性质，要求对所有时刻都满足：out必须等于表达式(in -> pre (in -> pre in))，即out与“对输入in进行两次逐拍延迟（带初值为当前in）”的结果完全一致。