定义top节点：  
- 输入：接收一个布尔类型的输入reset。  
- 输出：返回一个布尔类型的输出OK。  
- 内部变量：  
    - i：一个整数类型的内部变量，用于记录每时刻的计算结果。  
- 功能逻辑：  
    初始时刻i的值为1，之后每个时刻i的值等于前一时刻i的值乘以2000；OK的值为检查i是否大于0。  
- 验证规则：  
    - OK：i大于0，使用kind2验证语法检测是否恒成立。