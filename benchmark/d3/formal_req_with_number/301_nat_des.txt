定义min_total_distance函数：  
- R1: 输入：接收三个整数类型的输入x1、x2、x3。
- R2: 输出：返回一个整数类型的输出distance。
- 内部变量：无。
- 功能逻辑：
    - R3: 计算三个朋友需要移动的最小总距离，他们应在某一点相遇；
    - R4: 这等于将三个数排序后，中间的那个数到两边两个数的距离之和。
- 验证规则：
    - R5: distance大于等于0；
    - R6: x1=x2=x3时，distance等于0。