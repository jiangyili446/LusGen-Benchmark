节点fact接收一个布尔类型输入（该输入不参与计算），输出一个整数F，并在节点内部定义整数变量N。

节点内部通过递推方式生成自然数序列N：初始时刻N取0；从下一时刻开始，N每个周期在上一个周期的基础上加1，即依次为0、1、2、3……。

输出F用于生成一个由N及其上一时刻值计算得到的整数序列：初始时刻F取1；从下一时刻开始，F按递推关系计算为“当前N乘以上一时刻的N”，即F = N * pre(N)。因此该序列依次为1、0、2、6、12……。
