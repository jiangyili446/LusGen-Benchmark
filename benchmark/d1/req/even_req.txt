节点Even接收一个整型输入N，并返回一个布尔输出B。B的计算逻辑为：当且仅当当前时刻N对2取模等于0时，B为true；否则B为false。也就是说，B用于指示当前输入N是否为偶数。

节点EvensFrom接收一个整型输入X，并返回一个整型输出E。E表示一个从初始输入X开始生成的、严格递增的偶数序列，包含所有大于X的偶数，并按从小到大的顺序输出。其行为为：在初始时刻，如果X是偶数则E等于X+2，否则E等于X+1（即取大于X的第一个偶数）；在后续每个时刻，E都在上一时刻的基础上加2，即E始终保持为偶数并每步递增2。