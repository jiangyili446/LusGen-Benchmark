定义Since节点：  
- 输入：接收两个布尔类型的输入X和Y  
- 输出：返回一个布尔类型的输出Z  
- 内部变量：无  
- 功能逻辑：Z的值由以下逻辑决定：  
    - 如果X在当前时刻为真，则Z为真  
    - 如果X在当前时刻为假，则需要满足以下条件才能将Z设为真：  
        - Y在当前时刻为真  
        - 从当前时刻开始，Y必须一直保持为真直到后续时刻结束  
    - 另外，Z的状态还受到之前状态的影响：  
        - 如果Y为真，并且在任何时刻Z的前一个状态为假，则可以将Z的状态设为真  
- 验证规则：无