定义fact节点：  
- 输入：接收一个布尔类型的输入i  
- 输出：返回一个整数类型的输出F  
- 内部变量：  
    - N：一个整数类型的内部变量，用于记录时刻计数  
- 功能逻辑：  
    - N的计算逻辑是：初始时为0，之后每个时刻N的值为前一时刻N的值加1  
    - F的计算逻辑是：初始时F的值为1，之后每个时刻F的值为当前时刻N的值乘以前一时刻F的值  
- 验证规则：无