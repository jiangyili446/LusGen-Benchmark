定义type_decl函数：  
- 输入：接收两个整数类型的输入i1和i2。  
- 输出：返回一个pair类型的输出x。  
- 类型定义：  
    - alias：为int类型的别名。  
    - pair：为结构体类型，包含两个字段，a为alias类型，b为int类型。  
    - color：为枚举类型，包含三个值blue、white和black。  
- 功能逻辑：x为pair结构实例，其中成员a赋予i1的值，成员b赋予i2的值。  
- 验证规则：无。