定义DownCounter节点：  
- 输入：  
    - 接收一个整数类型的输入Time  
    - 接收一个布尔类型的输入Restart  
- 输出：  
    - 返回一个整数类型的输出Ctr  
- 内部变量：无。  
- 功能逻辑：  
    在初始时刻，Ctr的值为Time。  
    如果输入Restart为真，则Ctr的值被重置为Time；  
    如果Restart为假且之前时刻的Ctr大于0，则Ctr的值在之前时刻的基础上减去1；  
    如果以上条件都不成立，则保持Ctr的值不变。  
- 验证规则：无。