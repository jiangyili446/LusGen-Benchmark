定义Switch节点：  
- 输入：接收三个布尔类型的输入Set、Reset和Init  
- 输出：返回一个布尔类型的输出X  
- 内部变量：无  
- 功能逻辑：X的值根据以下逻辑计算：  
    - 如果Set为真，则X为真；  
    - 如果Reset为真，则X为假；  
    - 如果Set和Reset都为假，则根据Init信号决定X的状态：  
        - 如果Init为真，X保持为之前的值；  
        - 如果Init为假，X状态保持不变；  
- 验证规则：无